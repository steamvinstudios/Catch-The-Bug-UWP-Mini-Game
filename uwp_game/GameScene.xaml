<Page
    x:Class="uwp_game.GameScene"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:uwp_game"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Loaded="Page_Loaded">

    <Grid>
        <StackPanel Padding="20" CornerRadius="20" Canvas.ZIndex="-1" Background="#111111" HorizontalAlignment="Left" VerticalAlignment="Top">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="🪲X" FontSize="64"/>
                <TextBlock x:Name="counter" FontSize="64"/>
            </StackPanel>
            
            <StackPanel Orientation="Horizontal" Canvas.ZIndex="-1">
                <TextBlock Text="❤️X" FontSize="64"/>
                <TextBlock x:Name="health" FontSize="64"/>
            </StackPanel>
        </StackPanel>
        <Canvas x:Name="rootCanvas"/>
        
    </Grid>
</Page>
